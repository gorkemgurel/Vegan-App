//
//  ThumbnailPreviewView.swift
//  Vegan-2
//
//  Created by Gorkem Gurel on 16.08.2023.
//

/*import Foundation
import SwiftUI

struct ThumbnailPreviewView: View {
    var index: Int
    //var image: Image
    @ObservedObject var recipeAddViewModel: RecipeAddViewModel
    
    var body: some View {
        GeometryReader { geometry in
            //image
            Button(action: {
                print(self.index)
            }) {
                Image(uiImage: recipeAddViewModel.selectedPhotos[self.index])
                //image
                    .resizable()
                    .scaledToFit()
                    .frame(width: geometry.size.width, height: geometry.size.width)
            }
        }

    }
}

struct ThumbnailPreviewView_Previews: PreviewProvider {
    static var previews: some View {
        ThumbnailPreviewView(index: 0, recipeAddViewModel: RecipeAddViewModel())
    }
}
*/
